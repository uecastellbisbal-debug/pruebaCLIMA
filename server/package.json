{
  "name": "wellnessmeter-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API backend for the WellnessMETER survey + knowledge base admin panel.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build": "npm install --prefix ../client && npm run build --prefix ../client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
